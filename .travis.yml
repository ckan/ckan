language: python
python:
  - "2.7"
install: "pip install -r pip-requirements.txt --use-mirrors"
before_script:
  - psql -c 'create database ckan_test;' -U postgres
  - psql -c 'create database datastore_test;' -U postgres
script: "nosetests --ckan --with-pylons=test-travis.ini ckanext/jsonpreview/tests"