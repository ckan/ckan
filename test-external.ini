# Alternative config to test.ini that doesn't override the database used 
#
# It is used by tests external to ckan, such as ckanext-importer.
#

[DEFAULT]
debug = true

[server:main]
use = egg:Paste#http
host = 0.0.0.0
port = 5000

[app:main]
use = config:development.ini
