New `package_revise` action for safe concurrent multi-user dataset
updates. This action defines a new pattern for action parameters and
return values, and supports multiple file uploads in a single call.
