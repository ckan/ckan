* ``data-site-root`` and ``data-locale-root`` moved to
  ``site-root``/``locale-root`` meta elements inside ``head``. It means that
  code like ``$('body').data('site-root');`` must be replaced with
  ``$('meta[name=site-root]').attr('content');``
